# Audit of the 41 track records

`rg -l '^kind: track$' issues | wc -l` returned 41. Each record appears once below.

| track | verdict | current-tree reading |
|---|---|---|
| `audio/a-client-cannot-tell-soundd-it-paused` | **STANDS** | The shared audio header still exposes only producer/consumer indices (`toyos-abi/src/audio.rs:35-45`); there is no client-intent word to distinguish pause from starvation. |
| `audio/client-ring-depth-is-the-devices-pipeline-depth` | **STANDS** | soundd still sets `slot_count` directly from `num_buffers` (`userland/soundd/src/main.rs:178-202`); negotiation has not separated client depth from device depth. |
| `audio/hda-has-no-jack-detection-volume-or-keys` | **DRIFTED** | The claim that no pin-sense verb exists is stale: `GET_PIN_SENSE` exists, and static path setup already emits amp-gain/mute verbs (`toyos-hda/src/verb.rs:100-108`, `toyos-hda/src/config.rs:114-128`). Runtime sensing/routing, a user volume message, keys, and persistence remain wanted. |
| `build/clippy-has-never-run-here` | **DRIFTED** | Clippy now gates the host workspace, kernel, bootloader, and `toyos-abi` (`.github/workflows/host-tests.yml:200-220`). The remaining userland/toolchain limitation is stated at `.github/workflows/host-tests.yml:130-137`; the original whole-tree premise no longer matches. |
| `build/defect-events` | **STANDS** | The nearest structured record, `redlist`, models measurements and standing (`src/redlist.rs:13-30`) but does not record origin, discoverer, or escape boundary. No code-backed defect-event instrument supplies those axes. |
| `build/i8042-health-sits-on-the-ten-second-line` | **STANDS** | The unsafe tier straddle is already contained by a `Why::Cost` row, while the row still names the performance debt and its current measured guard (`src/tiers.rs:1061-1077`). The remaining “remove the excess cost” work is still real. |
| `build/soundds-mix-pass-has-no-host-test` | **STANDS** | The real period loop remains in `soundd::mix_thread` (`userland/soundd/src/mix.rs:120`, `:245-285`); the pure crate tests component decisions, but no host seam runs the pass that composes wait, commands, client pulls, and submission. |
| `build/the-eased-merge-law-carries-a-threshold` | **SUPERSEDED** | CI now triggers on `merge_group` (`.github/workflows/ci.yml:24-31`), and the measurement code explicitly distinguishes queued from eased regimes by reading the merge-queue rule (`src/mergehealth.rs:261-307`). Queue composition made the eased-law threshold historical rather than live policy. |
| `build/the-initrd-is-still-the-root-filesystem` | **STANDS** | The bootloader still hands an initrd to the kernel (`bootloader/src/main.rs:549-560`), and the kernel mounts it as the read-only root (`kernel/src/main.rs:423-428`). No GPT root handoff replaced it. |
| `build/the-swarm-is-not-yet-falsifiable` | **STANDS** | `mergehealth` deliberately reports counts but does not classify the failed test or interaction mechanism (`src/mergehealth.rs:23-29`). The broader task-to-merge, defect-origin, cost, and comparative-control instrument remains unbuilt. |
| `build/the-toolchain-ships-no-cargo-and-the-shared-cache-waits-on-one` | **STANDS** | The custom toolchain still provisions cargo as a symlink to the host binary (`src/toolchain.rs:918-929`); no self-contained cargo or safe shared cross-worktree crate cache replaces that dependency. |
| `build/there-is-no-network-gate` | **STANDS** | Network tests exist, but a source search across workflows, harness, kernel, userland, and host crates found no pcap/tcpdump/filter-dump oracle. The current gates do not independently read what reached the virtual wire. |
| `build/toyos-cc-has-never-compiled-tcc` | **STANDS** | C discovery still scans only the TinyCC corpus as input (`tests/toyos.rs:1450-1452`); no builder compiles the TinyCC compiler itself with `toyos-cc`. |
| `build/toyos-cc-has-no-codegen-gate` | **STANDS** | The emission suite explains that the corpus and determinism checks missed a real wrong store and then checks selected encodings (`toyos-cc/tests/emission.rs:1-9`, `:62-70`). There is still no systematic instruction/ABI code-generation gate. |
| `design-debt/redesign-the-log-subsystem` | **DRIFTED** | The record's six-file/no-core and 39-flat-file picture is obsolete: the tree now has a `kernel/src/log/` core with eight Rust files and `logd` as an independent user process (`kernel/src/log/mod.rs:19-64`, `userland/logd/src/main.rs:1-10`). A command count found 50 top-level Rust files, so the remaining layout/backpressure work needs a rewritten baseline. |
| `diagnostics/no-cyclictest` | **STANDS** | The RT entry syscall exists (`toyos-abi/src/syscall.rs:250-253`), but searches of userland and tests found no cyclic latency program or histogram gate using it. |
| `diagnostics/nothing-in-the-machine-can-read-the-trace-ring` | **STANDS** | The trace ring is explicitly an LLDB-read symbol (`kernel/src/trace.rs:1-5`, `:141-148`); no syscall or in-machine reader exposes it. |
| `diagnostics/the-kernel-keeps-nothing-it-enumerates` | **STANDS** | PCI enumeration returns a temporary vector (`kernel/src/drivers/pci.rs:275`; consumed at `kernel/src/main.rs:350`), and no persistent hardware registry/identity service is built. |
| `diagnostics/the-log-staged-three-things-it-never-built` | **STANDS** | `logd` still claims no service port and only the `logread` system capability (`system.toml:33-40`). A persistent RAM sink, structured query service, and remote sink remain absent from the shipped log path. |
| `hardware/a-metal-session-runs-a-pre-flash-gate-first` | **STANDS** | The image builder only asserts sector-aligned flashability (`src/image.rs:397-405`). Searches of build commands and workflows found no command that binds a hardware pre-flash verdict to the exact artifact about to be written. |
| `hardware/device-shape-and-lifecycle-have-no-coverage` | **DRIFTED** | The “nothing built” lifecycle claim is stale: the harness has real QMP `device_add`/`device_del` operations (`tests/common/qemu.rs:3472-3500`) and a committed `xhci_hotplug` arm (`tests/common/usb.rs:2230`, `tests/toyos.rs:916`). The broader shape matrix, removal-under-I/O, reclaim, and independent hardware-boundary oracles remain wanted. |
| `hardware/the-bot-scsi-machine-is-still-hand-written-in-the-kernel` | **STANDS** | BOT/SCSI remains inside the kernel, including its one-command controller lock (`kernel/src/drivers/xhci/wait/msc.rs:1-20`). `toyos-xhci` contains the pure port/protocol/recovery machine, not mass-storage BOT/SCSI (`toyos-xhci/src/lib.rs:1-23`). |
| `hardware/the-t14-touchpad-is-i2c-hid-and-unbuilt` | **STANDS** | Searches across the kernel, host device crates, userland, and tests found no I2C-HID or Intel LPSS implementation; the existing input stack remains i8042/USB HID. |
| `hardware/there-is-no-wifi` | **STANDS** | Searches across kernel, userland, host crates, and tests found no 802.11/WLAN implementation. `virtio_net` is the adjacent wired NIC driver (`kernel/src/drivers/mod.rs:18`), not Wi-Fi. |
| `isolation/the-power-broker-authority-with-a-human-in-the-loop` | **STANDS** | The ABI still gives a POWER holder a direct, non-returning shutdown call (`toyos-abi/src/syscall.rs:1088-1101`; dispatched at `kernel/src/arch/syscall/dispatch.rs:169`), and no broker/consent protocol stands between that holder and ACPI shutdown. |
| `kernel/all-cores-are-assumed-equal-and-arm64-breaks-that` | **STANDS** | Spawn placement rotates a start CPU and asks only the load-based placement rule (`kernel/src/sched/driver.rs:383-390`); no per-core capacity enters it. |
| `kernel/arm64-is-a-decision-nobody-has-made` | **STANDS** | The declared build targets remain the three x86_64 targets (`src/toolchain.rs:101`), with x86_64-only kernel and UEFI target files (`kernel/rust-toolchain.toml:2`, `bootloader/rust-toolchain.toml:2`). No ARM64 kernel target or policy exists. |
| `kernel/bcachefs-crate-is-not-bcachefs` | **STANDS** | The kernel still mounts the project's `bcachefs::SliceBlockIO` implementation as root (`kernel/src/main.rs:423-428`), whose on-disk superblock declares the project-specific `BCFS` magic and version 1 (`bcachefs/src/superblock.rs:1-6`). No upstream bcachefs implementation or compatibility oracle replaced it. |
| `kernel/cpu-time-is-a-band-and-not-a-reservation` | **STANDS** | The scheduler still carries band-era tripwires explicitly described as superseded by the unbuilt reservations design (`kernel/src/scheduler.rs:529-543`). No budget/period admission ledger is present. |
| `kernel/every-driver-is-still-in-the-kernel` | **STANDS** | All shipping device modules remain under `kernel/src/drivers` (`kernel/src/drivers/mod.rs:7-22`); no userspace driver boundary has displaced them. |
| `kernel/every-interrupt-lands-on-the-boot-cpu` | **STANDS** | PCI MSI/MSI-X still uses the fixed cpu0 message address (`kernel/src/drivers/pci.rs:21-25`). The census measures that fact, but no destination policy changes it. |
| `kernel/every-wait-in-this-kernel-is-a-spin` | **DRIFTED** | The record's opening state is obsolete: completion/parking machinery exists and some waits yield, while BOT still holds a controller across a synchronous command (`kernel/src/drivers/xhci/wait/msc.rs:1-20`). The remaining async conversions and lock walls are real, but the “every wait”/four-site baseline no longer describes this tree. |
| `kernel/fairness-is-per-thread-so-thread-count-wins` | **DRIFTED** | The headline premise is false now: all threads of a process share one vruntime (`toyos-sched/src/fair.rs:95-99`), backed by a `Pid -> KShare` map (`kernel/src/scheduler.rs:249-268`). Hierarchical capability-domain shares and reservations remain unbuilt. |
| `kernel/nothing-charges-kernel-memory-to-a-process` | **STANDS** | `ProcessAccounting` has faults, I/O, blocking, CPU, and runqueue counters but no kernel-allocation ownership (`kernel/src/process.rs:547-562`); `ProcessStats`' memory fields do not supply per-process kernel-memory charging (`toyos-abi/src/syscall.rs:1873-1896`). |
| `kernel/page-global-is-a-decision-nobody-has-made` | **STANDS** | The paging code explicitly says there is no `PAGE_GLOBAL` mapping bit (`kernel/src/mm/paging.rs:324-335`), and PGE is not among required/optional CR4 features (`kernel/src/arch/control_regs.rs:54-60`). No policy or measurement has made the decision. |
| `kernel/placement-is-blind-to-caches-and-topology` | **STANDS** | Placement rotates a starting CPU and selects by scheduler load (`kernel/src/sched/driver.rs:383-390`); no cache, NUMA, or topology input participates. |
| `kernel/the-capability-end-state-is-twelve-answers` | **DRIFTED** | Several once-open answers are now enforced (for example `ProcessStats.pid` documents the MANAGE-gated identity rule at `toyos-abi/src/syscall.rs:1882-1886`), while the record still mixes old “open” prose with later rulings. The remaining model/spec work is real, but “twelve unanswered answers” is no longer a current baseline. |
| `kernel/the-fair-band-orders-by-share-alone-and-a-woken-editor-waits` | **STANDS** | The shipped fair state is still vruntime/share based (`toyos-sched/src/fair.rs:75-99`), and no latency/deadline reservation changes the ordering of an interactive wake. |
| `kernel/the-iommu-stops-at-translation` | **STANDS** | VT-d gives every PCI function an identity-mapped domain and enables translation (`kernel/src/iommu/vtd/mod.rs:1-8`); interrupt remapping and isolation-domain policy are not present. |
| `kernel/the-kernel-still-parses-what-userland-writes` | **STANDS** | Process creation still parses untrusted ELF in the kernel loader (`kernel/src/loader/mod.rs:1-8`), and shared-object effects remain in kernel ELF code (`kernel/src/elf/mod.rs:1-7`). No userspace parser/service boundary replaced them. |
| `kernel/the-scheduler-and-the-clocks-never-talk` | **STANDS** | The machine clock is calibrated once from HPET and then read from TSC (`kernel/src/clock.rs:1-4`, `:19-51`); there is no scheduler feedback into frequency/idle-state policy. |

## Counts

- STANDS: 33
- DONE: 0
- SUPERSEDED: 1
- DRIFTED: 7
