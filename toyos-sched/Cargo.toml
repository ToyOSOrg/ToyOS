# This crate, `sim/` and `loom/` are three members of the host workspace (root
# `Cargo.toml`), which is where all three are listed. They used to be a
# workspace of their own here; the set of host-tested crates is one list now,
# and it is that one.

[package]
name = "toyos-sched"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[features]
# Host-side additions for the simulator and tests. Never enabled by the kernel.
std = []
# Deep cross-container invariant asserts (see `src/invariants.rs`). Off by default;
# enabled by sim runs and kernel check builds.
check = []
# The byte-level stray-write tripwire on `CpuSched` — the `tripwire` impl block
# in `src/cpu.rs` carries what it is for. Off by default and never on in a
# measured build: it copies and compares the whole record at every entry to the
# driver's exclusive region. It compiles readers and no decision, so a build
# carrying it schedules exactly as one without it.
tripwire = []
# Compiles the escape hatch `old_steal_port` needs to re-create the OLD
# scheduler's steal-in-transit algorithm. The simulator enables
# it; the kernel does not, so the old protocol is not reachable from
# production code at all.
protocol-port = []
# Declared, never enabled here: the `toyos-sched-loom` package compiles these
# same sources into its own crate with the feature on, so `src/sync.rs`
# resolves to loom's instrumented atomics. Keeping it a feature of a separate
# package (rather than a `cfg(loom)` dependency) keeps loom and its 30
# transitive host crates out of `kernel/Cargo.lock` — still true, and the
# reason it is: `kernel/` is excluded from the host workspace and resolves
# against its own lockfile, and nothing the kernel builds depends on
# `toyos-sched-loom`.
loom = []
# Declared, never enabled here: `toyos-sched-loom` turns this on to relax the
# doorbell's kick-pending edge in `mailbox.rs`'s `Doorbell::ring`, and
# `loom/tests/loom_sleep.rs` must red when it does — the sleep handshake's own
# negative control. No kernel build reaches it; the crate
# declares it only so `cfg` checking knows the name.
doorbell-kick-relaxed = []
# Declared, never enabled here: `toyos-sched-loom` turns this on to weaken the
# `SeqCst` fence `cpu.rs`'s `balance_fence` puts between the push half's surplus
# store and its read of a target's SLEEPING bit, and
# `loom/tests/loom_push.rs` must red when it does — `Balance::PushOnSurplus`'s
# own negative control. No kernel build reaches it, and no kernel build selects
# that policy either; the crate declares the name only so `cfg` checking knows
# it.
push-fence-relaxed = []
# The staleness test's negative control: `cpu.rs`'s `CpuHandle::answering`
# answers `true` for every CPU under it, which is what every path that chooses a
# CPU believed before it existed. The simulator's
# `a_stopped_cpu_stops_taking_work` must red when it is on. No kernel build
# selects it, and one that did would place tasks onto CPUs that never pick
# them up.
placement-ignores-staleness = []
