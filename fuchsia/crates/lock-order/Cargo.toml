[package]
name = "lock-order"
version = "0.1.0"
edition = "2024"
license = "BSD-2-Clause"

[lib]
path = "../../upstream/src/connectivity/network/netstack3/core/lock-order/src/lib.rs"

[features]

[dependencies]

[lints.rust]
unexpected_cfgs = { level = "allow" }
