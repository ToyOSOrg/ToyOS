# ToyOS fork manifest
#
# Every third-party crate ToyOS patches. Nothing is vendored: each fork is a
# real repository with a `toyos` branch based on a pinned upstream commit, so
# `git log <base>..toyos` is always the exact ToyOS delta — the artifact an
# upstream PR is made of.
#
# Tiers:
#   sibling  — upstream needs only a small cfg-gated dispatch arm; the real
#              code lives in a ToyOS-owned crate. Target state for platform
#              backends: the fork eventually disappears.
#   fork     — the change is genuinely invasive and must live in the crate.
#   toolchain— consumed by the rust/ submodule rather than userland.
#   source   — not a crate: third-party source this project compiles, pinned to
#              an upstream commit and fetched rather than patched. doomgeneric
#              is the only one. The tier exists because this manifest is the
#              estate's inventory, and a third-party source tree that is not in
#              it is precisely the hole the manifest closes.
#
# `base` is the upstream commit the branch is built on. Several are NOT release
# tags: the vendored snapshots predated the tags they claimed, and using the tag
# would have misattributed upstream churn to us. Each base was found by
# minimising the diff and is recorded here so re-basing is reproducible.
#
# `pr` is the upstream pull request, or why there isn't one yet. Most forks wait
# on ToyOS's own crates reaching crates.io (toyos-abi, toyos, toyos-window): a
# fork cannot reference them by path once cargo checks it out alone. The
# exceptions are the forks whose ToyOS code depends on nothing of ours — a
# target triple, a handle type — and those are the ones already sent. A git
# dependency on toyos-abi or toyos is the same violation and worse: unlike a
# path dependency it resolves, silently, against whatever revision the git ref
# last pointed to — a frozen snapshot standing in for the monorepo's copy rather
# than a build error.
#
# `.github/workflows/publish.yml` is what unblocks them: it publishes the five
# on every landing that moves a version, and `src/sdkversion.rs` is the rule
# that keeps those versions moving. What is left is the owner's crates.io token
# as a repository secret, then the `owed` edits below, one per fork.
#
# A fork being sendable is not the same as it being *worth* sending. See memmap2
# below: purely additive, unblocked, and still a bad PR.
#
# `delta` is `git diff --shortstat <base>..origin/<branch>`, re-measured
# 2026-08-07, and it describes the *branch head* rather than whatever a lockfile
# pins. The numbers before 2026-07-28 were inflated by vendoring cruft — gutted
# upstream READMEs (memmap2's deleted the upstream authors' copyright notices)
# and stray .gitignore entries — which has now been reverted everywhere. The
# invariant is that base..branch contains no hunk an upstream PR would not:
#   git diff <base>..origin/<branch> -- README* LICENSE* COPYRIGHT NOTICE .gitignore
# and expect empty. It was empty for all sixteen branches on 2026-08-07.
# Fork identity lives in each repo's GitHub description, which costs no diff.
#
# Where a fork has an open PR, the fork repo carries a *second* branch at the PR
# head, named below as `pr_branch`. It is not redundant with `toyos`: the PR sits
# on whatever upstream commit the maintainer wants and carries only PR content,
# while `toyos` stays on `base` and may carry local-build accommodations that
# `not_pr_content` records. `toyos` is the branch every [patch] entry consumes.

[meta]
owner = "ToyOSOrg"
branch = "toyos"
# Local dev: clone a fork next to the monorepo and add a path override in
# .cargo/config.toml for the duration; the committed [patch] entries always
# point at the git branches.

[socket2]
upstream = "rust-lang/socket2"
base = "v0.6.3"
delta = "+1293/-11"
tier = "fork"
why = "Full socket backend (src/sys/toyos.rs) over the ToyOS network syscalls."
pr = "none — sendable once toyos-abi and toyos are on crates.io"

[mio]
upstream = "tokio-rs/mio"
base = "844cf25"
base_note = "two commits past v1.2.0; the tag itself gives a polluted diff"
delta = "+775/-30"
tier = "fork"
why = "io_uring-driven selector, pipe waker, ToyOS TCP types."
pr = "none — sendable once toyos-abi and toyos are on crates.io"

[tokio]
upstream = "tokio-rs/tokio"
base = "d7db722b"
base_note = "three commits past tokio-1.51.1"
delta = "+118/-41"
tier = "fork"
why = "cfg-gated ToyOS arms in TcpSocket; unsupported APIs disabled."
pr = "none — sendable once toyos-abi and toyos are on crates.io"
not_pr_content = "The root Cargo.toml carries a `[patch.crates-io] mio = <our fork>` entry. It only affects builds *inside* the tokio repo (consumers resolve via tokio/Cargo.toml), so it may be load-bearing for local dev there — but it is never PR content and needs a decision."

[russh]
upstream = "Eugeny/russh"
base = "v0.60.0"
delta = "+703/-231"
tier = "fork"
why = "ToyOS bits are small; most of the delta is a pure-Rust `rustcrypto` cipher backend."
pr = "none — the rustcrypto backend is independently upstreamable TODAY (no ToyOS dependency); split it out and send it"

[cpal]
upstream = "RustAudio/cpal"
base = "v0.18.0"
base_note = "45fe976. The `toyos` branch on b4ddd28 is left behind, unrewritten."
branch = "toyos-0.18.0"
delta = "+326"
tier = "sibling"
why = "Pure addition: a ToyOS host backend. Should become a `cpal-toyos` crate."
pr = "none — sendable once toyos-abi and toyos are on crates.io"
owed = "nothing for the rename: Cargo.toml's toyos arm names `toyos-abi = \"0.1\"` and `toyos = \"0.1\"`, both unchanged."
release_note = """
b4ddd28 is 94 commits before the tag whose version number its manifest already
carried, so the backend implemented traits no released cpal has. On v0.18.0 the
per-operation error enums are one `Error` of `ErrorKind` plus an optional
message, `DeviceTrait` requires `Display` and has no `name`, `DeviceId`'s fields
are private behind `DeviceId::new`, and the dispatched `Devices` derives
`Clone`. The two ToyOS commits cherry-pick (one conflict, in the null host's
docsrs cfg list, which the release deleted); the host's own migration is the
third commit on the branch. The advertised configuration, the three asserts
against soundd's reading of the device, and the fill loop are unchanged, so no
consumer's audio arithmetic moved.
"""

[winit]
upstream = "rust-windowing/winit"
base = "v0.31.0-beta.2"
base_note = "f98ccf8. The `toyos` branch on c4afadbf, 57 commits past the tag, is left behind, unrewritten."
branch = "toyos-0.31.0-beta.2"
delta = "+1249"
tier = "sibling"
why = "Adds the winit-toyos sibling crate — the model case for this tier."
pr = "none — winit-toyos must be published before upstream can depend on it by version"
followup = "winit-toyos is ToyOS's own code still living inside the fork; move it to the monorepo once published"
release_note = """
All seven ToyOS commits cherry-picked clean and `winit-toyos`'s tree is
byte-identical to the one on `toyos`: beta.2's `winit-core` carries every API it
names. The 57 commits the old base stood on moved `winit-core` only by
documentation, an `Ohos` variant added to `NativeKeyCode`/`NativeKey`, an
`Error` impl on `ImeSurroundingTextError`, and a `Send + Sync` bound added to
`OwnedDisplayHandle` — a bound the release does not ask for and this backend
therefore does not have to satisfy.
"""
rwh_patch = """
This fork's own root manifest carries a `[patch.crates-io]` for
raw-window-handle, and it names the `toyos-0.6.2` branch: the crate resolves
raw-window-handle 0.6, and the master-based `toyos` branch was handing every
dependent the unreleased API that goes with it. The patch stays until #223 is
released.
"""

[softbuffer]
upstream = "rust-windowing/softbuffer"
base = "v0.4.8"
base_note = "d871852. The `toyos` branch on master's f0d20b9 is left behind, unrewritten."
branch = "toyos-0.4.8"
delta = "+155"
tier = "sibling"
why = "ToyOS surface backend."
pr = "none — sendable once toyos-window is on crates.io"
release_note = """
The backend was re-applied by hand rather than cherry-picked: v0.4.8's
`BufferInterface` is `pixels`/`pixels_mut` over `&[u32]` with `resize` and
`buffer_mut`, where master's is `Pixel`, `AlphaMode`, `configure` and
`byte_stride`. The buffer is `width * height` with no padding, so present blits
row by row through `window::Framebuffer::blit`, which clips to the window.
"""
rwh_patch = """
The `[patch.crates-io] raw-window-handle` entry stays until #223 is released:
0.6.2 has no `ToyOs` variant, so `src/backends/toyos.rs` cannot name one. It
names the fork's `toyos-0.6.2` branch — the release plus that variant — so
v0.4.8's own `Web` match arms are the right ones and the delta is additive.
"""

[raw-window-handle]
upstream = "rust-windowing/raw-window-handle"
base = "v0.6.2"
base_note = "5fda8e8. The `toyos` branch on master's 1b85948 is left behind, unrewritten, and so is `add-toyos-support`, which is where the maintainer wants the PR."
branch = "toyos-0.6.2"
delta = "+112"
tier = "sibling"
why = "Pure addition: a ToyOS handle variant. Depends on nothing of ours, so it was sendable without publishing."
pr = "https://github.com/rust-windowing/raw-window-handle/pull/223 (open, 2026-07-28)"
pr_branch = "add-toyos-support"
pr_note = "`toyos` was moved to c39042b so its tree is byte-identical to the PR head; the delta grew +69 → +109 by adding the doc examples and CHANGELOG entry every other platform module carries. Verified 2026-08-07: `git diff origin/toyos origin/add-toyos-support` is empty, and both are +109 off 1b85948."
release_note = """
Re-applied by hand and not cherry-picked: v0.6.2 predates upstream's owned
handles and its `Web*` → `WasmBindgen*` rename, so the module list and the
thread-safety asserts sit differently. `src/toyos.rs` is byte-identical to
`toyos`'s and every ToyOS line in `src/lib.rs` is the same text; the +3 over
`toyos`'s +109 is the `## Unreleased` heading a release branch needs.
"""

[getrandom]
upstream = "rust-random/getrandom"
base = "v0.2.17 / v0.3.4 / v0.4.2"
branches = ["toyos-0.2", "toyos-0.3", "toyos-0.4"]
delta = "+14 / +19 / +19"
tier = "fork"
why = "Three major versions are live in the dependency graph simultaneously."
pr = "none — trivial delta, and the first candidate once toyos-abi is on crates.io"

[libloading]
upstream = "nagisa/rust_libloading"
base = "35b6a30"
base_note = "0.9.0 plus two upstream commits; Cargo.toml version is relabelled 0.8.9 to satisfy a rustc pin"
delta = "+341/-4"
tier = "toolchain"
why = "dlopen/dlsym over ToyOS."
pr = "none — sendable once toyos-abi is on crates.io"

[memmap2]
upstream = "RazrFalcon/memmap2-rs"
base = "v0.9.10"
base_note = "Cargo.toml version is relabelled 0.2.1 to satisfy rustc_data_structures' pin"
delta = "+173/-3"
tier = "toolchain"
why = "ToyOS mapping emulation in its own cfg-gated module."
pr = "deliberately NOT sent — see pr_note. Purely additive, unblocked, now correct, and still a bad PR."
pr_note = """
Evaluated 2026-07-28 by building the port against upstream master and running
upstream's own suite against our module. It failed 5 of 16 tests, which turned
up three real bugs in our branch — all now fixed, see `bugs` below. The original
first objection (the fork violated memmap2's own ErrorKind::Unsupported contract
from #161 by reporting success and discarding writes) is therefore RESOLVED, and
the note's own escape clause is satisfied verbatim. Three objections remain, and
each is independently decisive:
  1. No pattern to follow. memmap2 has no per-target_os module arm at all, only
     unix/windows/stub. This would invent the dispatch rather than join it —
     the opposite of the target-lexicon and raw-window-handle cases.
  2. Upstream could never compile or test it: target_os="toyos" exists only in
     the rust/ fork, unlike Redox and Haiku which are real rustc targets.
  3. Nothing upstream blocks us. ToyOS is neither unix nor windows, so stock
     memmap2 already routes us to stub.rs and builds.
It is also barely a platform port: ToyOS has no mmap, so the module is a generic
heap-buffer emulation any platform could have.
"""
bugs = """
Three bugs, found by that evaluation and fixed in c74a0a9 (branch now 87ae85a):
  1. read_file used file.try_clone() and seeked — try_clone SHARES the offset,
     so mapping silently reseeked the caller's File to the end of the mapped
     range and every later read returned wrong data or EOF.
  2. Mapped pointers were byte-aligned (backing Vec<u8>) where the crate
     guarantees usize alignment, including for empty maps.
  3. map_anon treated its 4th parameter as a fill byte; upstream's signature is
     `huge: Option<u8>`, a huge-page size shift. `.huge(Some(n)).map_anon()`
     returned a buffer full of byte n instead of zeros.
map_mut/map_exec/make_mut-on-shared now return Unsupported rather than pretending
to succeed. Decided empirically: nothing in the graph can reach them (see
`dead_code`), so Unsupported cannot break a caller, while emulated write-back
would have made ToyOS depend on semantics that are not mapping semantics.
"""
dead_code = """
OPEN QUESTION. rust/compiler/rustc_data_structures/src/memmap.rs cfg-gates
target_os="toyos" to a Vec<u8> implementation at all 8 sites, and rust/Cargo.toml
is now the only manifest that patches memmap2 at all — userland used to carry a
duplicate entry that resolved to nothing and warned on every build. So NO ToyOS
code path calls any memmap2 API: the 165 lines of src/toyos.rs are compiled and
unreachable, and the fork's only load-bearing content is the 0.9.10 → 0.2.1
version relabel.
Two coherent end-states, exactly one of which should exist:
  (a) delete src/toyos.rs and let stub.rs serve ToyOS, keeping the relabel; or
  (b) drop the toyos gate in rustc_data_structures now that map_copy_read_only
      and map_anon — the only two memmap2 APIs rustc uses — are correct here.
Keeping both is the duplication the zero-technical-debt principle forbids.
"""

[stacker]
upstream = "rust-lang/stacker"
base = "93c7abc"
base_note = "0.1.23 was released without a tag"
delta = "+86/-6"
tier = "toolchain"
pr = "none — sendable once toyos-abi is on crates.io"
not_pr_content = "`cc` is relaxed 1.2.33 → 1.2.0 in both Cargo.toml and psm/Cargo.toml — a build-dep downgrade with no ToyOS connection, 4 of the 8 files in the delta. Undocumented: find out what forced it (MSRV of the pinned toolchain?) and either justify or drop it."

[ctrlc]
upstream = "Detegr/rust-ctrlc"
base = "3.5.2"
base_note = "vendored Cargo.toml claimed 3.5.1; the code is 3.5.2. The version string should be restored."
delta = "+42/-1"
tier = "toolchain"
pr = "none"

[target-lexicon]
upstream = "bytecodealliance/target-lexicon"
base = "v0.13.5"
delta = "+7/-1"
tier = "fork"
why = "Adds the toyos target triple. Five lines of code across two files."
pr = "https://github.com/bytecodealliance/target-lexicon/pull/134 (open, 2026-07-27)"
pr_branch = "add-toyos-os"
pr_note = "Unlike raw-window-handle the two branches cannot be made identical: `add-toyos-os` sits on a later upstream (it carries #131 and #133) and is +5/-1, while `toyos` stays on v0.13.5 and is +7/-1. The ToyOS hunks in src/targets.rs and src/triple.rs are the same on both; the extra two lines are `not_pr_content`."
not_pr_content = "`#![allow(unexpected_cfgs)]` in build.rs and src/lib.rs. build.rs:56 emits `cargo:rustc-cfg=feature=\"rust_1_40\"`, a feature the manifest never declares, so the lint fires on a crate that denies warnings. It is an upstream lint debt, not ToyOS content — fixing it upstream is its own PR, and doing so would let this fork drop to +5/-1."

[doomgeneric]
upstream = "ozkl/doomgeneric"
base = "fc601639494e089702a1ada082eb51aaafc03722"
base_note = """
"I_Error exits instead of infinite loop", 2025-08-24. Not a tag — doomgeneric
publishes none. Chosen because it is what this project has been building all
along: of the 202 files in the checkout that produced every doom measurement on
record, the 197 nobody had touched match this commit's `doomgeneric/` tree
exactly, and it is the newest commit for which that is true. Identified by
hashing each file and comparing against every commit on master.
"""
delta = "none"
tier = "source"
why = """
The C half of /bin/doom — 56,726 lines descended from id Software's Doom, via
Chocolate Doom. `userland/doom/build.rs` fetches the commit's archive into
`userland/doom/doomgeneric/`, which is gitignored, and compiles it with
`toyos-cc`. Every ToyOS-side change lives beside it rather than in it:
`userland/doom/include/` is force-included ahead of the C tree and
`userland/doom/src/` is the Rust side, so the fetched sources are upstream's
unmodified and `delta` is honestly zero.
"""
pin = """
The commit is the pin and the checksum both: GitHub's archive of a sha is that
sha's tree and can be nothing else, so no second hash of ours would say more.
`userland/doom/doomgeneric/.toyos-commit` records what a checkout actually has,
and a mismatch replaces the tree. Before 2026-08-08 the fetch was
`refs/heads/master` guarded by whether the directory existed, so which Doom
sources you built was a function of when you first cloned.
"""
licence = "GPL-2.0 — the one exception the README's licence section already declares."
pr = """
none, and none is owed: nothing of ours is in the tree to send back.
"""
followup = """
This is a fetch, not a fork, and the two differ where it matters: there is no
`toyos` branch, so a ToyOS patch to the C would have nowhere to live but an
untracked directory — which is where the one that existed had been living. The
end state the audit recommends is Japabu/doomgeneric as a submodule at that
path, which deletes the fetch and the five build-dependencies that serve it
(`ureq`, `rustls-rustcrypto`, `webpki-roots`, `flate2`, `tar`). It needs a repo
the owner creates, so it is his call rather than an agent's.
"""

# Not a crate fork: rust/ is a submodule of Japabu/rust (compiler + std with
# ToyOS as a target). Its own upstream sync is tracked separately.
