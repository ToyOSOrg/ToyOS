[package]
name = "toyos-logstream"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[dependencies]
# `MAX_RECORD_MESSAGE`, which the queue's bound is derived from.
toyos-abi = { path = "../toyos-abi" }
