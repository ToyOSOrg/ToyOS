# Audit of the 21 READY judges

Scope is the 21 entries under `READY` in the committed `READINESS.md` from the codex6 audit. The verdict asks about the judge present in this tree, not whether the defect looks locally fixable.

| defect | verdict | audit |
|---|---|---|
| `filesystem/fat-replace-rename-swallows-its-rollback-failure` | **TOO-WEAK** | The host test injects exactly one failure, at the source move, then requires both names and the independent checker to survive (`toyos-fat32/tests/replace_rename.rs:47`, `:68-116`). It accepts the wrong fix that restores the destination after that failure but still swallows a *second* device failure during the restore; the claimed two-refusal arm does not exist. |
| `filesystem/usb-esp-gate-holes` | **EXISTS-UNWIRED** | `esp_filesystem` already rereads the built ESP through host `fatfs` and runs `toyos-fat32-check` before the guest (`tests/common/volumes.rs:246-281`). The remaining read comparator/digest, negative parser assertion, and mis-stamped-guard cases are not connected to that mechanism. |
| `kernel/dlopen-dedup-only-holds-after-the-race-settles` | **TOO-WEAK** | `dlopen_dedup` performs its repeated opens sequentially and compares their handles/TLS (`tests/toyos-rust-tests/src/bin/dlopen_dedup.rs:20-64`). It accepts the wrong implementation that allocates two modules when two first opens race and deduplicates only after either has registered. |
| `kernel/granularity-bound-crossed-at-four-widths` | **TOO-WEAK** | The simulator test run by `cargo test` sweeps only widths 1 and 2 (`toyos-sched/sim/tests/scenarios.rs:668-690`). It accepts the shipped/wrong behavior at the affected wider widths; the CLI's ability to run another width does not make those widths a committed judge. |
| `filesystem/readdir-bound-is-per-mount` | **TOO-WEAK** | The existing test fills `/home` and separately exercises `/tmp` subdirectories (`tests/toyos-rust-tests/src/bin/readdir_bound.rs:41-92`), but never fills one mount and lists an empty directory on another. It therefore accepts a wrong mount-wide/global bound in place of a per-mount bound. |
| `build/nothing-checks-the-dependency-bar` | **EXISTS-UNWIRED** | `sourcegate` already performs source-absence gates during `cargo test --lib` (`src/sourcegate.rs:1-19`), and `forkcheck` parses git dependencies throughout manifests (`src/forkcheck.rs:377-395`). No ledger connects those mechanisms to every external crate, binary, generated asset, and host dependency named by this defect. |
| `build/the-shard-split-prices-a-boot-and-not-the-image-behind-it` | **TOO-WEAK** | `Shard::keep` and its unit tests prove unique, complete cost partitioning of the lists they are handed (`src/testargs.rs:41-64`, `:356-430`). They accept the wrong fix that continues to omit the separate image/config build cost, because that cost is not an input to the assertion. |
| `build/the-build-system-does-not-compile-on-windows` | **WORKS** | The nightly portability workflow now runs `cargo build -p toyos-build` on `windows-latest` (`.github/workflows/portability.yml:330-357`). That compiles the real host crate on the platform where the unguarded Unix imports fail, so a local Unix-only repair stays red while a repair that makes the crate compile on Windows turns this exact job green. |
| `audio/desktop-session-put-26ms-of-silence` | **EXISTS-UNWIRED** | Pure DLL/timing tests exist in `toyos-mixer`, while the choice between the stale estimate and the actual future timer target remains inside soundd (`userland/soundd/src/mix.rs:250-274`, `:367-381`). The deadline choice has not been factored into the pure mechanism, so those tests cannot currently judge it. |
| `audio/stop-the-device-voice-keep-the-wake` | **TOO-WEAK** | The null sink deliberately has no DMA pipeline or device submit (`userland/soundd/src/mix.rs:617-633`), and the shipped-client desktop arm explicitly uses that device-less sink (`tests/toyos.rs:7051-7109`). Both accept the wrong fix that stops a physical voice and loses the command-pipe wake, because neither arm observes a stopped physical voice. |
| `boot-media/log-flush-retry-reds-two-ways-at-two-in-five` | **EXISTS-UNWIRED** | The typed redlist/query mechanism exists and distinguishes a measurement from an exemption (`src/redlist.rs:13-35`), and `log_flush_retry` is dispatched (`tests/toyos.rs:8161`). A search for the exact typed-row form `test: "log_flush_retry"` finds no row in `src/redlist.rs`, so the local exit is not wired to its named judge. |
| `build/free-memory-verdicts-share-a-boot` | **EXISTS-UNWIRED** | The affected lifetime test still judges reclamation through machine-wide `SYS_SYSINFO` free bytes (`tests/toyos-rust-tests/src/bin/handle_lifetime.rs:173-208`); the per-kind same-process census exists separately (`tests/toyos-rust-tests/src/bin/handle_kill_policy.rs:306-336`). The free-memory verdicts do not consume that census. |
| `design-debt/what-is-owed-on-file-size` | **EXISTS-UNWIRED** | Adjacent one-byte/truncation boundary judges exist for ELF tables (`toyos-elf/tests/tables.rs:375-397`), while ACPI still parses firmware tables in the kernel. No ACPI extraction path feeds those host boundary tests, so the mechanism exists but is not connected to the remaining claim. |
| `diagnostics/blocked-time-is-invisible-while-the-park-lasts` | **TOO-WEAK** | The dump can already render live parked state, but `ProcessStats` copies only the accumulated five blocked counters (`kernel/src/process.rs:1013-1030`; ABI fields at `toyos-abi/src/syscall.rs:1873-1895`). Extending only `blocked_dump` with `now - parked_at` accepts the wrong fix in which the public `ProcessStats` values still remain frozen until wake. |
| `filesystem/a-shrink-unflushed-regrows-the-old-tail` | **TOO-WEAK** | The device-backed arms fsync each size transition; the only unflushed arm is `/tmp` (`tests/toyos-rust-tests/src/bin/fs_transactional.rs:127-163`, `:221-230`). A fix that handles durable or explicitly failed flushes but leaves the no-flush `/home` and `/log` sequence broken passes. |
| `filesystem/a-spawn-reads-round-an-open-file-s-dirty-pages` | **TOO-WEAK** | `writeback_spawn` writes through `fs::write`, which drops the handle before spawning (`tests/toyos-rust-tests/src/bin/writeback_spawn.rs:45-59`). It accepts a wrong fix that drains closed-file writeback but still reads around the dirty pages of a handle deliberately kept open. |
| `isolation/libc-loses-the-kernels-word-on-three-write-paths` | **ABSENT** | The C discovery root is only `tests/testcases/tinycc` (`tests/toyos.rs:1450-1452`), and `NOTICE:205-217` identifies `tests/testcases` as third-party compiler input. I searched the C discovery code and all C files under `tests/testcases`; there is no first-party C test location or harness arm in which the proposed `send`/`sendto`/`fwrite` versus `write(2)` oracle can exist. The nearby Rust guest tests cannot exercise this libc's C wrappers. |
| `isolation/undefined-flag-bits-cross-the-boundary-and-are-dropped` | **EXISTS-UNWIRED** | `endowment_denied` has the correct base/unknown-bit differential for `NamespaceBuild` (`tests/toyos-rust-tests/src/bin/endowment_denied.rs:152-207`). The other affected syscall request types are not connected to copies of that differential. |
| `kernel/process-open-panics-on-a-reopened-process` | **TOO-WEAK** | The existing `process_open` arm supplies a capability without `MANAGE` and stops at `PermissionDenied` (`tests/toyos-rust-tests/src/bin/process_lifecycle.rs:266-277`). It accepts the wrong implementation that behaves for unauthorized callers but panics when an authorized caller reopens a retired process. |
| `kernel/soundd-past-due-wake-max-1` | **TOO-WEAK** | Gate A records wake/completion statistics after the wait (`userland/soundd/src/mix.rs:350-393`), but no host test drives the past-due calculation and the implementation still clamps the computed timeout with `.max(1)` (`userland/soundd/src/mix.rs:256-274`). The current gate accepts that wrong line unchanged. |
| `panic-path/panic-console-capture-untested` | **TOO-WEAK** | `screen_late_panic` proves that the fatal report paints (`tests/toyos.rs:3993-4015`), not that it came from the frozen snapshot. The implementation itself records that no test distinguishes capture's absence (`kernel/src/drivers/panic_console/mod.rs:465-472`), so replacing `capture()` with a no-op can still pass—the required negative control. |

## Counts

- WORKS: 1
- EXISTS-UNWIRED: 7
- TOO-WEAK: 12
- ABSENT: 1
