[package]
name = "shell"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[dependencies]
toyos = { path = "../../toyos" }
# `launch_path`, which is the other half of the gate init refuses a launch on.
toyos-manifest = { path = "../../toyos-manifest" }
