[package]
name = "terminal"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[dependencies]
toyos = { path = "../../toyos" }
toyos-font = { path = "../toyos-font" }
toyos-window = { path = "../toyos-window" }
