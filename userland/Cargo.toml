[workspace]
resolver = "2"
members = [
    "calc",
    "compositor",
    "console",
    "doom",
    "editor",
    "filepicker",
    "filepicker-api",
    "files",
    "init",
    "input-test",
    "logd",
    "netd",
    "paint",
    "proctest",
    "shell",
    "snake",
    "soundd",
    "sprite",
    "sshd",
    "terminal",
    "test-runner",
    "toybox",
    "toyos-font",
    "toyos-window",
]
exclude = [
    # no_std C runtime — uses panic=abort (own Cargo.toml profile)
    "libc",
]

[patch.crates-io]
# ToyOS's own crates: the forks below depend on these by version so they can be
# consumed as git dependencies (a path escaping the fork's own repo cannot
# resolve once cargo checks it out alone). These redirects keep local builds
# on the working tree. Publishing them to crates.io is only needed for real
# upstream PRs, not for building here.
toyos-abi = { path = "../toyos-abi" }
toyos = { path = "../toyos" }
toyos-window = { path = "toyos-window" }

# Ecosystem forks — see forks.toml for upstream base, tier, and PR status.
getrandom = { git = "https://github.com/ToyOSOrg/getrandom", branch = "toyos-0.2", package = "getrandom" }
getrandom_03 = { git = "https://github.com/ToyOSOrg/getrandom", branch = "toyos-0.3", package = "getrandom" }
getrandom_04 = { git = "https://github.com/ToyOSOrg/getrandom", branch = "toyos-0.4", package = "getrandom" }
mio = { git = "https://github.com/ToyOSOrg/mio", branch = "toyos" }
tokio = { git = "https://github.com/ToyOSOrg/tokio", branch = "toyos" }
russh = { git = "https://github.com/ToyOSOrg/russh", branch = "toyos" }
russh-cryptovec = { git = "https://github.com/ToyOSOrg/russh", branch = "toyos" }
socket2 = { git = "https://github.com/ToyOSOrg/socket2", branch = "toyos" }
raw-window-handle = { git = "https://github.com/ToyOSOrg/raw-window-handle", branch = "toyos" }
cpal = { git = "https://github.com/ToyOSOrg/cpal", branch = "toyos" }
softbuffer = { git = "https://github.com/ToyOSOrg/softbuffer", branch = "toyos" }
winit = { git = "https://github.com/ToyOSOrg/winit", branch = "toyos" }
libloading = { git = "https://github.com/ToyOSOrg/rust_libloading", branch = "toyos" }


# The one profile every guest binary is built with.
# Optimised, because an unoptimised guest mismeasures everything under TCG;
# debug-assertions and overflow-checks on, because fail-fast beats speed here
# and both crafted-ELF kernel panics in `issues/` were *found* by
# an overflow check. `--release` would turn the last two off silently, so this
# build system no longer has the flag.
#
# What it replaces here: dependencies at 2 through `package."*"`, doom and the
# compositor named individually at 2, and every other program at the dev
# default of 0 — so the compositor's own code was optimised and the terminal's
# was not. One opt-level for the whole image needs no per-package override.
[profile.toyos]
inherits = "dev"
opt-level = 2
debug = true
debug-assertions = true
overflow-checks = true
