# A member of the host workspace (root `Cargo.toml`): the one scratch directory
# the tree takes, which is gone on every way out of the process that made it.

[package]
name = "toyos-tmpdir"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
publish = false

[lints.rust]
warnings = "deny"
