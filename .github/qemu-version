# The QEMU version every CI guest and the dev host run — read by `ci.yml`'s
# shards, `tcg` and `cache-writer`, `gate-a.yml`, and the build system's
# prerequisite check. The version decides test outcomes — 8.2.2 and 11.0.3 were
# measured disagreeing about the same tree — so this is a declaration
# `instrument.sh` holds the image to, never a fact read off it. The date in
# `.github/apt-snapshot` is chosen to satisfy this line; changing this line is
# a deliberate act: it says the instrument moved.
11.1.0
