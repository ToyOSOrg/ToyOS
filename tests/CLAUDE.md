# Tests

The mechanics live where the work is: profiles and shapes in `tests/common/`, registration, tiers and `EXPECTED_FAILURES` in `tests/toyos.rs`, instruments and known reds in `src/redlist.rs`, the relegation record in `src/tiers.rs` — read those, not this file, for how the harness works.

## Caveats that bite every agent

- **The dev host is a laptop that sleeps mid-session, and the suite says so.** A run whose wall clock jumped against the monotonic one reports `INVL` per test and exits 2 — re-run rather than investigate. A wild outlier *not* marked that way is a real finding.
- **A landing-gate red on a test that is green alone is not therefore the host.** What a duration still decides prints `STALL` — red, and named apart so nobody bisects it. `ALONE: red again` on a loaded host means nothing without a same-session A/B against `main`. None of this re-runs an audio harm verdict away.
- **The converse holds too**: a machine-wide kernel panic reds whichever test was running — that red's name is the workload, never the cause, even when the isolated re-run reds again. `QEMU died before ===READY=== (status 0)` is the same thing said silently: a guest that reset itself under the harness's `-no-reboot` — a kernel death, never a host kill (that is a signal) nor a QEMU that could not start (that is non-zero) — so its name is the workload and its evidence is the boot log, which the arm that reports it currently drops.
- **A machine-wide death during boot is reproduced by boots, not by suites** — twelve parallel `bootable.img` guests is ~1,500 boots in fifteen minutes; a suite loop measures the suite, not the death. The per-guest wait is on the completion marker, never a fixed timer — a timer at that width manufactures phantom reds by killing guests still in firmware. An arm's baseline is measured in the same session as the arm, never carried from an earlier one: one day's `main` halved the prior session's rate, and every verdict against the stale number would have been wrong. A death is counted whether or not the guest printed a marker — three quarters of this class's deaths exit QEMU silently, and a marker-only count undercounted 4× for two days; run guests with `-action reboot=shutdown -action shutdown=pause` so a silent death parks with its registers readable over QMP. A measurement on the T14 records whether a CI job container shared the machine, because the runner is also the instrument — a block that gains company mid-run is discarded and re-run, never corrected. A defect whose rate is set by interrupts per unit of guest work is measured on the *slowest* instrument available: the dev host's TCG showed in 7,059 boots what 17,555 KVM boots on real silicon never did, so real hardware can be the weaker oracle.
- **`ALONE: GREEN — its Sched::Parallel is wrong` is a hypothesis, not a finding** — a red whose mechanism is a kernel race must not be answered by re-classifying its `Sched`; the harness suggests scheduling, the mechanism decides.
- **Gate A's thorough tier reds on the dev host, and the fast tier intermittently** — stash and re-run before believing a red is yours, and note a plain `cargo test` boots no audio config at all; both configs are `Tier::Nightly`. The nightly workflow's own red was never evidence either way until 2026-08-21 (a shell construct masked every verdict it ever produced): read the verdict line, not the check status.
- **A C test's capture has other processes' lines removed from it before comparison**, on the boot config's own list of who may speak (`tests/common/console.rs`); a program whose output has no trailing newline is joined to the next writer's line by the host's splitter, and that is undone there too.
- **`/bin/init` speaks in every program's name before that program runs** — `init: netd: no nic on this machine` is in the capture before netd exists — so a predicate keyed on a `<program>: ` prefix is satisfied by the wrong speaker. Wait for the whole line, in the constant the assertion also reads.
- **A guest binary cannot ask what a handle it does not hold does** — the probe ends its caller with exit 139, so it runs in a child, one fault per child, a printed marker proving the child reached the call. `handle_kill_policy` is the pattern.
- **A boot's capture has two pieces and they used to have a gap between them.** `boot_log()` ends at the ready marker and `run_test`'s `stdout`/`serial` begin at `===TEST_START===`; every console line in between now lands in `TestResult::before` and is dropped by any caller that ignores it. A daemon still finishing its startup writes into that window — measured at five lines, one `wall_clock_file` run in three — so a test that reads a daemon's boot line appends `before` to its capture. The harness ends every guest by killing QEMU, so anything the kernel prints only at `SYS_SHUTDOWN` reaches no capture — a kernel-side instrument has to speak at a moment a running guest reaches, and a process exit is the only recurring one (the interrupt census, 2026-08-22).
- **A file's last close no longer flushes or drops it synchronously** — the write-back queue hands the dirty pages to `iod` and the closing thread returns at once, so a test that reopens a file, or reads its backing device, expecting the just-closed writes to be visible must first let `iod` drain; without that it adopts the still-cached pages and proves nothing. `home_backing_revoked` is the test that had to learn this the day the queue landed (2026-08-23).
- **Every guest this host boots is TCG — one vendor's reading of the ISA.** Anything vendor-dependent is gated only by CI's KVM shards. TCG also prices instructions unlike hardware: an uncontended atomic read-modify-write on a hot path is the one that bites.
- **CI's `guest` lane is two different machines** — twelve hosted EPYC shards for every pull request, push and merge-queue ref, one T14 lane for a `workflow_dispatch` or a non-`ci.yml` schedule — and a duration measured on one does not transfer to the other: the profile in `tests/test-durations` was recorded hosted, and only a hosted run renders the tier verdict, which since 2026-08-22 it does on the pull request itself.
- **The dev host's guests boot `-cpu qemu64`, which has no PCID** — every `INVPCID` path in the kernel is dead locally and only a KVM runner with `-cpu host` executes one, so a change gated on a CPUID feature is unverified by a green local suite.
- **A new registered name costs two CI cycles, not one** — the `UNMEASURED` marker buys one measured run (the first `durations` red is by design), and splitting or re-tiering a name after seeing its price buys a second. Plan the round-trips; they are not surprises. A name is only Fast if it is committed under `FAST_COMMIT_MS`, four fifths of the ceiling — a price in the last fifth is decided by which partition ran it, so it reds whichever pull request measures it next rather than the one that introduced it. Since 2026-08-22 a landing renders the price verdict only for the names it registered or re-tiered; for every other name it is a warning on that run and a red on the nightly, so a tier finding about somebody else's test is fixed the next day rather than by the author who happened to measure it. A Rust guest test's registration is the existence of its file under `tests/toyos-rust-tests/src/bin/`, so adding, deleting or renaming one is a registration change with a price verdict attached.
- **A measured bound is asserted against the derivation, never against the measurement** — the measured number goes in the doc and the assert message, and a bound that has to be widened to pass is a finding, not a calibration. A test that asserts a kernel `Budget` never expires is asserting a bound the kernel does not promise — the verdict is the disjunction, and the red is only the outcome that is neither the answer nor the declared degradation (`screen_fatal_halt_composited`, 2026-08-22).
- **A harness field that can be silently inert is this suite's worst defect class** — an arm that does nothing, a run that is green, and every negative control staged through it proving nothing (`kernel_params` beside a staged `boot_image` was one until 2026-08-22). Where two options can describe the same guest, they refuse each other by name rather than one quietly winning; an image is asked what it is armed with.
- **Host suites** run with plain `cargo test` inside `toyos-sched/`, `toyos-ps2/`, `toyos-gpt/`, `toyos-elf/`, `toyos-cc/`, `toyos-ld/`, `toyos-hda/`, `toyos-pci/`, `toyos-xhci/`, `toyos-desktop/`, `toyos-keymap/`, `bcachefs/`, `kernel-loom/`, `toyos-userbound/`, `toyos-elide/`, `toyos-fat32/`, `toyos-fat32-check/`, `toyos-abi/`, `toyos-manifest/`, `toyos-wallclock/`, `toyos-mixer/`, `toyos-dma/`; `userland/sshd`, `userland/calc` and `userland/soundd` cross-compile and need the host triple (`cargo test --target "$(rustc -vV | sed -n 's/^host: //p')"`) — any userland crate is host-testable this way despite the `toyos` SDK, which is how soundd's seven tests ran nowhere until 2026-08-15; `calc`'s arithmetic is all decided there, so its 64 tests are the whole of what that program is gated by. `kernel-loom/` and `toyos-sched/loom` are the memory-ordering checks — x86 TSO hides a missing acquire edge from every guest test. `toyos/` is host-testable too — plain `cargo test` inside it, its `lib.rs` being `cfg_attr(not(test), no_std)` — so an SDK decision that is a function of a kernel word has a host gate and needs no guest (the `netd error` mapping ran nowhere until 2026-08-22).
