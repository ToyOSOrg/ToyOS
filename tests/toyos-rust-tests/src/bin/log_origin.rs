//! A program that says one line and ends, for where that line goes: `/log`,
//! the log `logd` serves, and the console — each under the name of the
//! program whose pipe it came out of. `log_program_line` runs it.

fn main() {
    println!("log origin nonce 7d1f3a");
}
