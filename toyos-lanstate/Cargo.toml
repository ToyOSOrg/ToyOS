# A member of the host workspace (root `Cargo.toml`), like toyos-tco: netd
# depends on it by path from the userland workspace, the guest job that asks
# netd does the same from the harness's, and its tests run on the host beside
# the judge that reads what they declare.

[package]
name = "toyos-lanstate"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
